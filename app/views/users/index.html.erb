<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% @users.each_with_index do |user, index| %>
  <p><%= user.first_name %> <%= user.last_name %> - <%= link_to 'show', "/users/#{user.id}" %></p>
<% end %>

<%= link_to "add a new user", "/users/new" %>
